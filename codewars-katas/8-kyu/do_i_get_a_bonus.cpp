#include <iostream>
#include <string>
#include <sstream>
using namespace std;

string bonus(int salary, bool bonus) {
    string returnString = "\0"; 
    if (bonus) {
    }
}

int main() {
    return 0;
}